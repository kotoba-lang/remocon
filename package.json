{
  "private": true,
  "scripts": {
    "test": "kbb --backend sci --classpath src:test -e \"(require '[clojure.test :as t] 'remocon.nec-test 'remocon.pronto-test 'remocon.match-test) (let [r (t/run-tests 'remocon.nec-test 'remocon.pronto-test 'remocon.match-test)] (when (pos? (+ (:fail r) (:error r))) (quit 1)))\""
  }
}
